pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                // Use 'sh' for Linux or 'bat' for Windows
                bat 'echo Hello World' 
            }
        }
    }
}
